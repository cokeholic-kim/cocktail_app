## 스타일 토큰 누락 점검 항목

### 수동 점검
- [ ] `src/app/(common)/components/uiTokens.ts` 변경 시 사용처 영향 범위를 기록했는가?
- [ ] 신규 화면에 `bg-*`, `text-*`, `border-*` 하드코딩 색상이 새로 추가되지 않았는가?
- [ ] `SearchBox`, `DataStateNotice`, 카드류 컴포넌트가 공통 토큰을 사용하고 있는가?
- [ ] 반응형 클래스(`sm:`, `md:`) 추가 시 기존 컴포넌트와 높이/간격 충돌이 없는가?

### 코드 리뷰 제출 시 첨부
- 점검한 파일 목록
- 제거/교체한 하드코딩 클래스 목록
- 적용되지 않은 하드코딩 클래스 사유(예: 임시 UI 실험, 외부 SDK 제약)
